spring:
  application:
    name: clinicafemina
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:clinicafemina}
    username: ${DB_USER:postgres}
    password: ${DB_PASS:88jr83w88}
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    show-sql: true
    properties:
      hibernate:
        "[format_sql]": true
        dialect: org.hibernate.dialect.PostgreSQLDialect
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
  quartz:
    job-store-type: memory
    properties:
      "[org.quartz.threadPool.threadCount]": 2

app:
  security:
    jwt:
      secret: ${JWT_SECRET:minha_chave_secreta_local_muito_longa_para_testes_clinica_crm_2026}
      expiration-ms: 86400000
  crypto:
    master-key: ${APP_ENCRYPTION_KEY_V1:0123456789abcdef0123456789abcdef}

  rabbitmq:
    host: ${RABBITMQ_HOST:localhost}
    port: ${RABBITMQ_PORT:5672}
    username: ${RABBITMQ_USER:guest}
    password: ${RABBITMQ_PASS:guest}
    virtual-host: ${RABBITMQ_VHOST:/}

  whatsapp:
    verify-token: ${META_WHATSAPP_VERIFY_TOKEN:sandbox}
    app-secret: ${META_WHATSAPP_APP_SECRET:sandbox}
    access-token: ${META_WHATSAPP_ACCESS_TOKEN:sandbox}
    phone-number-id: ${META_WHATSAPP_PHONE_NUMBER_ID:12345}
    graph-api-url: https://graph.facebook.com/v20.0

  darwin:
    api-url: ${DARWIN_API_URL:https://sandbox.api.darwin.health/v1}
    api-token: ${DARWIN_API_TOKEN:eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJsb2NhdGlvbklkIjoiNDQ4YmIyZmMtYTBiYi00NzgxLWE2OGYtY2Y4M2Y0NDM4MzcxIiwiaWF0IjoxNzc5Mzc2NjYxfQ.DaEn3p4kKAndJmWYPwEN0QDIiErvMUv6FTbS7YJnNcs}
    sync-cron: ${DARWIN_SYNC_CRON:0 0/15 * * * ?}
    page-size: ${DARWIN_SYNC_PAGE_SIZE:100}
    enabled: ${DARWIN_SYNC_ENABLED:true}